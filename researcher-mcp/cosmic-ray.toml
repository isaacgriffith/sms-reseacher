[cosmic-ray]
module-path = "researcher-mcp/src/researcher_mcp"
timeout = 30.0
excluded-modules = []
test-command = "uv run --package sms-researcher-mcp pytest researcher-mcp/tests/unit -x -q"

[cosmic-ray.distributor]
name = "local"
